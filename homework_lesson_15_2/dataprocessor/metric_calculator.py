import datetime
import pandas as pd
import pathlib
import uuid


class MetricCalculator:
    def __init__(
            self,
            processed_filenames: list[str],
            path: str,
            date: datetime.date,
            time: datetime.time,
            sku: str,
            warehouse: str,
            warehouse_cell_id: int,
            operation: str,
            invoice: str,
            expiration_date: datetime.date,
            operation_cost: float,
            comment: str
            # additional parameter expected ..?_index: dict,
            # additional parameter expected
    ):
        """
        Class MetricCalculator
        additional parameter expected ..?_index: dict,
        additional parameter expected
        """
        # additional variable expected
        # additional variable expected

    def create_index(self):  # additional parameter expected
        pass

    # def calculate_metric_?(self):  # additional parameter expected
    #     pass
    #
    # def ?(self)  # additional parameter probably expected
    #     pass
